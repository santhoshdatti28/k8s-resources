apiVersion: v1 
kind: Pod
metadata:
  name: resource
spec: 
  containers: 
  - name: cont-1
    image: nginx
    resources:
      requests:
        memory: "68Mi"
        cpu: "100m"
      limits:
        memory: "120Mi"
        cpu: "150m"